---
- hosts: all
  gather_facts: no
  vars:
  - x: ""
  tasks:
  - name: Wait for the vms to load
    local_action: wait_for host={{ inventory_hostname }} port=5985 state=started delay=0 timeout=600
  - name: Get IP
    raw: ipconfig
    register: x
  - name: Print Output
    debug: var=x
